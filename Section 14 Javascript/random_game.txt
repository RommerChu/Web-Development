var names = ["Angela", "Ben", "Jenny", "Michael", "Chloe"];

function whosPaying(names) {

var numP = names.length; // get the index number
var random = Math.floor(Math.random() * numP); //index multiply by random
var randonP = names[random];// going number to random person

return randonP + " is going to buy lunch today!";

}


whosPaying(names)